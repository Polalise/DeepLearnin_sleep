# Work Log - 2026-06-26

## Context

- Project: LifeSnaps Sleep Health Prediction
- Workspace: `C:\workSpace\DeepLearnin_sleep`
- User direction: Use MongoDB original source data instead of CSV files.

## Actions Completed

### 1. Read project guide

- Read `CODEX_GUIDE.md`.
- Confirmed project direction:
  - Predict sleep health using wearable, stress, emotion, activity, and context indicators.
  - Stress is an input/explanatory variable, not the prediction target.
  - Original BSON data should be accessed through local MongoDB when deeper feature engineering is needed.

### 2. Checked local repository state

- Confirmed repository initially contained:
  - `CODEX_GUIDE.md`
  - `.venv`
  - empty `notebooks/01_lifesnaps_eda.ipynb`

### 3. Verified MongoDB files and tools

- Confirmed MongoDB server executable exists:
  - `C:\Program Files\MongoDB\Server\8.0.25\bin\mongod.exe`
- Confirmed MongoDB restore tool exists:
  - `C:\Program Files\MongoDB\Tools\100.17.0\bin\mongorestore.exe`
- Confirmed original BSON files exist:
  - `fitbit.bson`
  - `sema.bson`
  - `surveys.bson`

### 4. Checked local MongoDB availability

- Verified local MongoDB port `127.0.0.1:27017` was open.
- Confirmed local MongoDB was reachable.

### 5. Installed Python dependencies

- Installed required packages into `.venv`:
  - `pymongo`
  - `pandas`

### 6. Created MongoDB raw overview script

- Added:
  - `scripts/01_load_mongodb_raw.py`
- Purpose:
  - Connect to `mongodb://localhost:27017`
  - Inspect database `rais_anonymized`
  - Check collections:
    - `fitbit`
    - `sema`
    - `surveys`
  - Write a compact overview report.
- Output report:
  - `reports/mongodb_raw_overview.md`

### 7. Confirmed MongoDB collection counts

- Confirmed document counts:
  - `fitbit`: 71,284,346 documents
  - `sema`: 15,380 documents
  - `surveys`: 935 documents

### 8. Created Fitbit type profiling script

- Added:
  - `scripts/02_profile_fitbit_types.py`
- Purpose:
  - Aggregate `fitbit` documents by `type`
  - Count documents per type
  - Save sample structure for each Fitbit type
- Output report:
  - `reports/fitbit_type_profile.md`

### 9. Profiled Fitbit raw data types

- Important Fitbit type counts found:
  - `heart_rate`: 48,720,040
  - `calories`: 9,675,782
  - `Wrist Temperature`: 4,372,238
  - `steps`: 3,010,529
  - `estimated_oxygen_variation`: 2,009,637
  - `Heart Rate Variability Details`: 220,512
  - `resting_heart_rate`: 12,362
  - `sleep`: 4,141
  - `Daily Heart Rate Variability Summary`: 2,475
  - `Stress Score`: 1,911

### 10. Inspected sleep document structure

- Confirmed `type == "sleep"` documents include:
  - `dateOfSleep`
  - `startTime`
  - `endTime`
  - `duration`
  - `minutesToFallAsleep`
  - `minutesAsleep`
  - `minutesAwake`
  - `minutesAfterWakeup`
  - `timeInBed`
  - `efficiency`
  - `mainSleep`
  - sleep stage summaries under `levels.summary`

### 11. Created sleep extraction script

- Added:
  - `scripts/03_export_sleep_from_mongodb.py`
- Purpose:
  - Query only `fitbit` documents where `type == "sleep"`
  - Flatten key sleep fields and sleep stage summary fields
  - Export to analysis-ready CSV
- Output file:
  - `data/raw/sleep_from_mongodb.csv`

### 12. Exported sleep data from MongoDB

- Export result:
  - Rows: 4,141
  - Columns: 37
  - Participants: 69
  - Date range: 2021-05-24 to 2022-01-22
  - Main sleep rows: 3,688

## Current Project State

- MongoDB original source access is working.
- Fitbit raw type distribution is documented.
- Sleep records have been extracted from MongoDB into a local analysis table.
- The next modeling dataset should start from `data/raw/sleep_from_mongodb.csv`, filtered to `mainSleep == True`.

## Recommended Next Step

1. Create a sleep target preparation script.
2. Load `data/raw/sleep_from_mongodb.csv`.
3. Filter to `mainSleep == True`.
4. Create:
   - `good_sleep_label = 1` when `efficiency >= 85` and `minutesAsleep >= 420`
   - `good_sleep_label = 0` otherwise
5. Summarize target distribution.
6. Then join additional MongoDB Fitbit types by participant and date:
   - `Stress Score`
   - `Daily Heart Rate Variability Summary`
   - `resting_heart_rate`
   - `steps`
   - `calories`

## Additional Work

### 13. Added MongoDB loading code to the Jupyter notebook

- Updated:
  - `notebooks/01_lifesnaps_eda.ipynb`
- Added notebook sections for:
  - MongoDB connection setup
  - Collection count checks
  - Fitbit sample document structure inspection
  - Fitbit `type` count aggregation
  - Loading only `type == "sleep"` documents from MongoDB
  - Flattening sleep documents into a pandas DataFrame
  - Saving loaded sleep data to `data/raw/sleep_from_mongodb.csv`
- Added detailed Korean comments explaining:
  - Why the full `fitbit` collection should not be loaded at once
  - Why MongoDB projection is used
  - How participant IDs are preserved for later joins
  - How nested sleep stage fields are safely extracted

### 14. Improved comments in existing scripts

- Updated comments in:
  - `scripts/01_load_mongodb_raw.py`
  - `scripts/02_profile_fitbit_types.py`
  - `scripts/03_export_sleep_from_mongodb.py`
- Comment improvements explain:
  - MongoDB connection assumptions
  - Why sampling is used for large raw collections
  - Why `type` profiling is needed before preprocessing
  - Why only `type == "sleep"` is exported
  - How sleep stage variants are handled

### 15. Validation

- Verified Python script syntax with `py_compile`.
- Verified the notebook is valid JSON.
- Verified notebook code cells parse as valid Python syntax.

### 16. Created project rule file

- Added:
  - `Codex.Rule.md`
- Purpose:
  - Preserve user-defined working rules so future Codex work in this project stays consistent.
- Rules documented:
  - When Codex creates or edits files, append the work details to the matching date file under `log/`.
  - Prefer MongoDB original source data over CSV for this LifeSnaps workflow.
  - Do not load the full large `fitbit` collection into pandas at once.
  - Add careful comments to notebook code and Python scripts, especially for data loading and preprocessing.
  - Continue the project flow from MongoDB raw inspection to sleep target creation, feature joins, preprocessing, and baseline modeling.

### 17. Expanded project rules for reporting and reproducibility

- Updated:
  - `Codex.Rule.md`
- Added rules for:
  - Preserving `data/raw/` as original or first-extract data.
  - Saving cleaned and engineered outputs under `data/processed/`.
  - Defining clear output filenames before major preprocessing work.
  - Writing or updating reports under `reports/` after major analysis steps.
  - Including data source, target definition, feature list, split method, metrics, results, and limitations in reports.
  - Using participant-aware split as the default modeling split.
  - Starting with simple baseline models before complex or deep learning models.
  - Moving finalized notebook logic into reproducible scripts.
- Next recommended action:
  - Start the first preprocessing step by creating a processed sleep target table from `data/raw/sleep_from_mongodb.csv`.
