# Work Log - 2026-06-28

## Actions Completed

### 1. Added notebook summary for script-based pipeline progress

- Added:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Purpose:
  - Organize the work completed in `scripts/` into a Jupyter notebook format.
  - Make the current pipeline progress easier to review for reporting and presentation.

### 2. Notebook contents

- The notebook summarizes these completed stages:
  - collection-level EDA
  - needed variable extraction
  - date-level aggregation
  - daily aggregate validation and visualization
  - merge into the daily modeling dataset
- The notebook points to the corresponding scripts:
  - `scripts/04_profile_collections_for_eda.py`
  - `scripts/05_extract_needed_variables.py`
  - `scripts/06_aggregate_daily_features.py`
  - `scripts/07_validate_visualize_daily_aggregates.py`
  - `scripts/08_merge_daily_modeling_dataset.py`
- The notebook also reads and summarizes generated outputs:
  - extracted variable CSV files
  - daily aggregate CSV files
  - `data/processed/modeling_dataset_daily.csv`
  - key reports under `reports/`
  - validation figures under `reports/figures/`

### 3. Validation

- Verified:
  - notebook JSON format is valid
  - notebook code cells parse as valid Python
  - notebook code cells execute successfully against existing generated files
- Result:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb` is ready for review.

## Current Pipeline Status

```text
컬렉션별 EDA 완료
→ 필요한 변수 추출 완료
→ 날짜 단위로 집계 완료
→ 집계 데이터 검증 및 시각화 완료
→ 세 데이터 병합 완료
```

## Next Recommended Action

```text
최종 데이터셋 EDA
→ 결측치 처리
→ 범주형 인코딩
→ 스케일링
→ PCA
```

## Additional Work

### 4. Ran final dataset EDA

- Added:
  - `scripts/09_final_dataset_eda.py`
- Generated:
  - `reports/final_dataset_eda.md`
  - `reports/figures/final_dataset_eda/target_distribution.png`
  - `reports/figures/final_dataset_eda/missing_rate_by_feature_family.png`
  - `reports/figures/final_dataset_eda/key_features_by_target.png`
  - `reports/figures/final_dataset_eda/top_target_correlations.png`

### 5. Final dataset EDA results

- Input:
  - `data/processed/modeling_dataset_daily.csv`
- Shape:
  - 3,551 rows
  - 130 columns
  - 69 participants
  - 116 numeric feature candidates, excluding keys and target columns
- Target distribution:
  - `good_sleep_label = 0`: 2,153 rows
  - `good_sleep_label = 1`: 1,398 rows
- Duplicate participant-date rows:
  - 0

### 6. EDA findings

- High-missing feature families:
  - SpO2: average missing rate 65.36%
  - stress: average missing rate 47.48%
  - sleep-stage columns: average missing rate 43.63%, max 94.56%
  - respiratory: average missing rate 37.65%
  - SEMA: average missing rate 32.55%
- Low-missing feature families:
  - activity: average missing rate 0.11%
  - resting heart rate: 0.00%
  - target columns: 0.00%
- Notes:
  - Classic sleep-stage columns have high missingness because Fitbit mixes classic and stages sleep formats.
  - Stress-related features may carry sleep/recovery leakage risk.
  - Record-count columns may be useful as coverage indicators.

### 7. Updated notebook summary

- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Added:
  - final dataset EDA script entry
  - final dataset EDA report preview
  - final dataset EDA figures
  - updated current pipeline status

### 8. Validation

- Ran:
  - `python scripts/09_final_dataset_eda.py`
  - `python -m py_compile scripts/09_final_dataset_eda.py`
  - notebook JSON validation
  - notebook code-cell syntax validation
  - notebook code-cell execution check
- Result:
  - Final dataset EDA completed successfully.
  - Notebook summary is updated and executable.

## Current Pipeline Status

```text
컬렉션별 EDA 완료
→ 필요한 변수 추출 완료
→ 날짜 단위로 집계 완료
→ 집계 데이터 검증 및 시각화 완료
→ 세 데이터 병합 완료
→ 최종 데이터셋 EDA 완료
```

## Next Recommended Action After Final EDA

```text
결측치 처리
→ 범주형 인코딩
→ 스케일링
→ PCA
```

## Additional Work - Missing Value Handling

### 9. Handled missing values for modeling features

- Added:
  - `scripts/10_handle_missing_values.py`
- Generated:
  - `data/processed/modeling_dataset_missing_handled.csv`
  - `data/processed/missing_value_feature_metadata.csv`
  - `reports/missing_value_handling_summary.md`

### 10. Missing-value handling strategy

- Input:
  - `data/processed/modeling_dataset_daily.csv`
- Output:
  - `data/processed/modeling_dataset_missing_handled.csv`
- Preserved:
  - `participant_object_id`
  - `calendar_date`
  - `good_sleep_label`
- Excluded from modeling features:
  - same-night sleep outcome columns such as `minutesAsleep`, `efficiency`, sleep-stage minutes/ratios
  - identifiers and timestamp columns
  - `stress_sleep_points_mean` because it is explicitly sleep-related
- Applied rules:
  - Drop feature columns with missing rate greater than 70%.
  - Add missing indicators for retained columns with missing values.
  - Fill count/rate/sum/record-count columns with 0.
  - Fill other retained numeric columns with median.

### 11. Missing-value handling results

- Input shape:
  - 3,551 rows
  - 130 columns
- Output shape:
  - 3,551 rows
  - 200 columns
- Output missing cells:
  - 0
- Duplicate participant-date rows:
  - 0
- Missing indicators added:
  - 97
- Dropped high-missing column:
  - `respiratory_light_sleep_breathing_rate_mean`

### 12. Updated notebook summary

- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Added:
  - missing-value handling script entry
  - missing-value handling summary section
  - report preview
  - current pipeline status update

### 13. Validation

- Ran:
  - `python scripts/10_handle_missing_values.py`
  - `python -m py_compile scripts/10_handle_missing_values.py`
  - output dataset shape/missing/duplicate checks
  - notebook JSON validation
  - notebook code-cell syntax validation
  - notebook code-cell execution check
- Result:
  - Missing-value handling completed successfully.
  - Output dataset has no missing values.
  - Notebook summary is updated and executable.

## Current Pipeline Status After Missing Handling

```text
컬렉션별 EDA 완료
→ 필요한 변수 추출 완료
→ 날짜 단위로 집계 완료
→ 집계 데이터 검증 및 시각화 완료
→ 세 데이터 병합 완료
→ 최종 데이터셋 EDA 완료
→ 결측치 처리 완료
```

## Next Recommended Action After Missing Handling

```text
범주형 인코딩
→ 스케일링
→ PCA
```

## Additional Work - Categorical Encoding

### 14. Finalized categorical encoding step

- Added:
  - `scripts/11_encode_categorical_features.py`
- Generated:
  - `data/processed/modeling_dataset_encoded.csv`
  - `data/processed/encoded_feature_columns.csv`
  - `reports/categorical_encoding_summary.md`

### 15. Encoding decision

- Input:
  - `data/processed/modeling_dataset_missing_handled.csv`
- Object columns found:
  - `participant_object_id`
  - `calendar_date`
- Decision:
  - Do not one-hot encode `participant_object_id`, because it is a grouping key for participant-aware split and could leak identity-specific patterns.
  - Do not one-hot encode `calendar_date`, because exact-date indicators are brittle and should remain a diagnostic/split key.
  - No categorical predictor columns were found.
- Result:
  - No one-hot encoded columns were added.
  - The step functions as feature table finalization before scaling/PCA.

### 16. Encoding results

- Input shape:
  - 3,551 rows
  - 200 columns
- Output shape:
  - 3,551 rows
  - 200 columns
- Feature columns:
  - 197 numeric features
  - 0 one-hot categorical features
- Output missing cells:
  - 0
- Duplicate participant-date rows:
  - 0

### 17. Updated notebook summary

- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Added:
  - categorical encoding script entry
  - categorical encoding summary section
  - report preview
  - current pipeline status update

### 18. Validation

- Ran:
  - `python scripts/11_encode_categorical_features.py`
  - `python -m py_compile scripts/11_encode_categorical_features.py`
  - output dataset shape/missing/duplicate checks
  - notebook JSON validation
  - notebook code-cell syntax validation
  - notebook code-cell execution check
- Result:
  - Categorical encoding / feature finalization completed successfully.
  - Notebook summary is updated and executable.

## Current Pipeline Status After Categorical Encoding

```text
컬렉션별 EDA 완료
→ 필요한 변수 추출 완료
→ 날짜 단위로 집계 완료
→ 집계 데이터 검증 및 시각화 완료
→ 세 데이터 병합 완료
→ 최종 데이터셋 EDA 완료
→ 결측치 처리 완료
→ 범주형 인코딩 완료
```

## Next Recommended Action After Categorical Encoding

```text
스케일링
→ PCA
```

## Additional Work - Scaling

### 19. Scaled model feature columns

- Installed:
  - `scikit-learn`
  - `joblib`
- Added:
  - `scripts/12_scale_features.py`
- Generated:
  - `data/processed/modeling_dataset_scaled.csv`
  - `data/processed/standard_scaler.joblib`
  - `data/processed/scaled_feature_columns.csv`
  - `data/processed/scaling_dropped_features.csv`
  - `reports/scaling_summary.md`

### 20. Scaling strategy

- Input:
  - `data/processed/modeling_dataset_encoded.csv`
- Preserved:
  - `participant_object_id`
  - `calendar_date`
  - `good_sleep_label`
- Applied:
  - `StandardScaler` to numeric model feature columns
- Removed before scaling:
  - zero-variance features, because they contain no information and are not useful for PCA
- Leakage note:
  - The current scaler is fit on the full dataset for preprocessing exploration.
  - In final model evaluation, scaling should be fit inside the training fold after participant-aware split.

### 21. Scaling results

- Input shape:
  - 3,551 rows
  - 200 columns
- Output shape:
  - 3,551 rows
  - 190 columns
- Scaled feature columns:
  - 187
- Dropped zero-variance features:
  - 10
- Output missing cells:
  - 0
- Duplicate participant-date rows:
  - 0
- Scaling validation:
  - max absolute scaled mean: approximately 0
  - max absolute scaled standard-deviation deviation from 1: approximately 0

### 22. Dropped zero-variance features

- Dropped:
  - `mood_anger_count`
  - `mood_fear_count`
  - `mood_joy_count`
  - `mood_sadness_count`
  - `mood_surprise_count`
  - `mood_anger_rate`
  - `mood_fear_rate`
  - `mood_joy_rate`
  - `mood_sadness_rate`
  - `mood_surprise_rate`

### 23. Updated notebook summary

- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Added:
  - scaling script entry
  - scaling summary section
  - report preview
  - current pipeline status update

### 24. Validation

- Ran:
  - `python scripts/12_scale_features.py`
  - `python -m py_compile scripts/12_scale_features.py`
  - scaled output shape/missing/duplicate/scaled mean/std checks
  - notebook JSON validation
  - notebook code-cell syntax validation
  - notebook code-cell execution check
- Result:
  - Scaling completed successfully.
  - Notebook summary is updated and executable.

## Current Pipeline Status After Scaling

```text
컬렉션별 EDA 완료
→ 필요한 변수 추출 완료
→ 날짜 단위로 집계 완료
→ 집계 데이터 검증 및 시각화 완료
→ 세 데이터 병합 완료
→ 최종 데이터셋 EDA 완료
→ 결측치 처리 완료
→ 범주형 인코딩 완료
→ 스케일링 완료
```

## Next Recommended Action After Scaling

```text
PCA
```

## Additional Work - PCA

### 25. Ran PCA dimensionality reduction

- Installed notebook support packages:
  - `nbformat`
  - `nbclient`
- Added:
  - `scripts/13_run_pca.py`
- Generated:
  - `data/processed/modeling_dataset_pca.csv`
  - `data/processed/pca_model.joblib`
  - `data/processed/pca_components.csv`
  - `data/processed/pca_explained_variance.csv`
  - `data/processed/pca_top_loadings.csv`
  - `reports/pca_summary.md`
  - `reports/figures/pca/explained_variance_ratio.png`
  - `reports/figures/pca/cumulative_explained_variance.png`
  - `reports/figures/pca/pc1_pc2_by_target.png`

### 26. PCA strategy

- Input:
  - `data/processed/modeling_dataset_scaled.csv`
- Feature source:
  - `data/processed/scaled_feature_columns.csv`
- Preserved:
  - `participant_object_id`
  - `calendar_date`
  - `good_sleep_label`
- Applied:
  - PCA on 187 scaled numeric model features
- Component selection:
  - retained enough leading principal components to explain at least 95% cumulative variance
- Leakage note:
  - The current PCA is fit on the full dataset for preprocessing exploration.
  - In final model evaluation, PCA should be fit inside the training fold after participant-aware split.

### 27. PCA results

- Input shape:
  - 3,551 rows
  - 190 columns
- Input feature columns:
  - 187
- Output shape:
  - 3,551 rows
  - 41 columns
- Selected PC columns:
  - 38
- Cumulative explained variance:
  - 0.9505
- Output missing cells:
  - 0
- Duplicate participant-date rows:
  - 0

### 28. Updated notebook summary

- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Added:
  - PCA script entry
  - PCA summary section
  - PCA variance/loadings preview
  - PCA report preview
  - PCA generated figures
  - current pipeline status update

### 29. Validation

- Ran:
  - `python scripts/13_run_pca.py`
  - `python -m py_compile scripts/13_run_pca.py`
  - PCA output shape/missing/duplicate/cumulative-variance checks
  - notebook JSON validation
  - notebook code-cell syntax validation
  - notebook top-to-bottom execution check with `nbclient`
- Result:
  - PCA completed successfully.
  - Notebook summary is updated and executable.

## Current Pipeline Status After PCA

```text
컬렉션별 EDA 완료
→ 필요한 변수 추출 완료
→ 날짜 단위로 집계 완료
→ 집계 데이터 검증 및 시각화 완료
→ 세 데이터 병합 완료
→ 최종 데이터셋 EDA 완료
→ 결측치 처리 완료
→ 범주형 인코딩 완료
→ 스케일링 완료
→ PCA 완료
```

## Next Recommended Action After PCA

```text
participant-aware train/test split and baseline modeling
```

## Additional Work - Participant Split and Baseline Modeling

### 30. Created participant-aware train/test split

- Added:
  - `scripts/14_create_participant_split.py`
- Generated:
  - `data/processed/splits/train_participant_split.csv`
  - `data/processed/splits/test_participant_split.csv`
  - `data/processed/splits/participant_split_assignments.csv`
  - `reports/participant_split_summary.md`

### 31. Split strategy and results

- Input:
  - `data/processed/modeling_dataset_encoded.csv`
- Split method:
  - `StratifiedGroupKFold`
  - grouped by `participant_object_id`
  - selected fold closest to 20% test rows and overall target balance
- Train split:
  - 2,944 rows
  - 55 participants
  - `good_sleep_label` mean: 0.3988
- Test split:
  - 607 rows
  - 14 participants
  - `good_sleep_label` mean: 0.3690
- Leakage checks:
  - participant overlap: 0
  - train duplicate participant-date rows: 0
  - test duplicate participant-date rows: 0

### 32. Trained first baseline models

- Added:
  - `scripts/15_train_baseline_models.py`
- Generated:
  - `data/processed/baseline_model_predictions.csv`
  - `data/processed/baseline_model_metrics.csv`
  - `data/processed/baseline_random_forest_feature_importance.csv`
  - `data/processed/baseline_models.joblib`
  - `reports/baseline_modeling_summary.md`
  - `reports/figures/baseline/metric_comparison.png`
  - `reports/figures/baseline/roc_curves.png`
  - `reports/figures/baseline/confusion_matrices.png`

### 33. Baseline model results

- Feature count:
  - 197 encoded features
- Models evaluated:
  - `dummy_most_frequent`
  - `logistic_regression`
  - `logistic_regression_pca_95`
  - `random_forest`
- Best model by balanced accuracy:
  - `logistic_regression`
- Best model test metrics:
  - accuracy: 0.7809
  - balanced accuracy: 0.7986
  - precision: 0.6532
  - recall: 0.8661
  - f1: 0.7447
  - roc auc: 0.8801
  - average precision: 0.8055
- Note:
  - `logistic_regression_pca_95` performed poorly relative to raw encoded features.
  - PCA was fit inside the train-only pipeline for that model, so this result is not caused by using the earlier full-data PCA output.

### 34. Updated notebook summary

- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Added:
  - participant-aware split script entry
  - baseline modeling script entry
  - split quality summary
  - participant assignment preview
  - baseline metrics table
  - best baseline summary
  - Random Forest feature importance preview
  - baseline figures
  - current pipeline status update

### 35. Validation

- Ran:
  - `python scripts/14_create_participant_split.py`
  - `python scripts/15_train_baseline_models.py`
  - `python -m py_compile scripts/14_create_participant_split.py scripts/15_train_baseline_models.py`
  - split overlap/shape checks
  - baseline metric checks
  - notebook JSON validation
  - notebook code-cell syntax validation
  - notebook top-to-bottom execution check with `nbclient`
- Result:
  - Participant-aware split completed successfully.
  - Baseline modeling completed successfully.
  - Notebook summary is updated and executable.

## Current Pipeline Status After Baseline Modeling

```text
컬렉션별 EDA 완료
→ 필요한 변수 추출 완료
→ 날짜 단위로 집계 완료
→ 집계 데이터 검증 및 시각화 완료
→ 세 데이터 병합 완료
→ 최종 데이터셋 EDA 완료
→ 결측치 처리 완료
→ 범주형 인코딩 완료
→ 스케일링 완료
→ PCA 완료
→ participant-aware train/test split 완료
→ baseline modeling 완료
```

## Next Recommended Action After Baseline Modeling

```text
model validation review, feature-set comparison, and tuned modeling
```

## Additional Work - Feature-Set Comparison and Tuned Modeling

### 36. Compared feature-set variants

- Added:
  - `scripts/16_compare_feature_sets.py`
- Generated:
  - `data/processed/feature_set_comparison_metrics.csv`
  - `data/processed/feature_set_definitions.csv`
  - `reports/feature_set_comparison_summary.md`
  - `reports/figures/feature_set_comparison/balanced_accuracy_by_feature_set.png`
  - `reports/figures/feature_set_comparison/roc_auc_by_feature_set.png`

### 37. Feature-set comparison strategy

- Reused:
  - `data/processed/splits/train_participant_split.csv`
  - `data/processed/splits/test_participant_split.csv`
- Compared feature sets:
  - `all_features`
  - `no_missing_indicators`
  - `no_availability_features`
  - `wearable_only`
  - `wearable_values_only`
  - `context_survey_only`
- Compared model families:
  - Logistic Regression
  - Random Forest
- Validation purpose:
  - check whether performance depends heavily on missing indicators or record/count-style availability features

### 38. Feature-set comparison results

- Best feature-set/model:
  - `wearable_only` + `logistic_regression`
- Best test metrics:
  - balanced accuracy: 0.8076
  - ROC AUC: 0.9046
  - F1: 0.7556
  - precision: 0.6996
  - recall: 0.8214
- Availability-feature check:
  - all-feature Logistic Regression balanced accuracy: 0.7986
  - no-availability-feature Logistic Regression balanced accuracy: 0.7750
  - gap: 0.0235
- Interpretation:
  - removing missingness/count signals did not strongly reduce Logistic Regression performance on this split
  - `context_survey_only` performed near or below baseline, so current predictive signal is mainly from wearable features

### 39. Tuned compact baseline models

- Added:
  - `scripts/17_tune_baseline_models.py`
- Generated:
  - `data/processed/tuned_model_metrics.csv`
  - `data/processed/tuned_model_cv_results.csv`
  - `data/processed/tuned_model_predictions.csv`
  - `data/processed/tuned_models.joblib`
  - `reports/tuned_modeling_summary.md`
  - `reports/figures/tuned_modeling/tuned_test_metrics.png`
  - `reports/figures/tuned_modeling/cv_vs_test_balanced_accuracy.png`
- Tuning method:
  - `StratifiedGroupKFold` on train split
  - grouped by `participant_object_id`
  - scoring: balanced accuracy
  - held-out participant test split used only for final comparison
- Implementation note:
  - an initial broader parallel grid ran too long
  - the script was revised to use a compact grid and single-process search for stable local execution

### 40. Tuned modeling results

- Best tuned candidate:
  - Logistic Regression
  - feature set: `wearable_only`
- Best params:
  - `model__C`: 1.0
  - `model__class_weight`: `balanced`
- Best test metrics:
  - CV balanced accuracy: 0.8359
  - test balanced accuracy: 0.8076
  - ROC AUC: 0.9046
  - F1: 0.7556
  - precision: 0.6996
  - recall: 0.8214
- Validation note:
  - because the held-out test split has only 14 participants, final reporting should include confidence intervals or repeated participant-aware resampling

### 41. Updated notebook summary

- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Added:
  - feature-set comparison script entry
  - tuned modeling script entry
  - feature-set comparison metrics
  - feature-set definitions summary
  - tuned model metrics
  - tuned CV results preview
  - feature-set and tuned-modeling figures
  - current pipeline status update

### 42. Validation

- Ran:
  - `python scripts/16_compare_feature_sets.py`
  - `python scripts/17_tune_baseline_models.py`
  - `python -m py_compile scripts/16_compare_feature_sets.py scripts/17_tune_baseline_models.py`
  - feature-set best-model checks
  - tuned best-model checks
  - notebook JSON validation
  - notebook code-cell syntax validation
  - notebook top-to-bottom execution check with `nbclient`
- Result:
  - Feature-set comparison completed successfully.
  - Tuned modeling completed successfully.
  - Notebook summary is updated and executable.

## Current Pipeline Status After Tuned Modeling

```text
컬렉션별 EDA 완료
→ 필요한 변수 추출 완료
→ 날짜 단위로 집계 완료
→ 집계 데이터 검증 및 시각화 완료
→ 세 데이터 병합 완료
→ 최종 데이터셋 EDA 완료
→ 결측치 처리 완료
→ 범주형 인코딩 완료
→ 스케일링 완료
→ PCA 완료
→ participant-aware train/test split 완료
→ baseline modeling 완료
→ feature-set comparison 완료
→ tuned modeling 완료
```

## Next Recommended Action After Tuned Modeling

```text
final validation report and model interpretation
```

## Additional Work - Final Baseline Validation and Interpretation

### 43. Validated and interpreted selected baseline model

- Added:
  - `scripts/18_validate_interpret_baseline_model.py`
- Generated:
  - `data/processed/baseline_logistic_coefficients.csv`
  - `data/processed/baseline_probability_calibration.csv`
  - `data/processed/baseline_participant_bootstrap_metrics.csv`
  - `reports/final_baseline_validation_report.md`
  - `reports/figures/final_baseline_validation/top_logistic_coefficients.png`
  - `reports/figures/final_baseline_validation/probability_calibration.png`
  - `reports/figures/final_baseline_validation/bootstrap_metric_intervals.png`

### 44. Validation and interpretation scope

- Selected baseline:
  - `wearable_only + Logistic Regression`
- Model source:
  - `data/processed/tuned_models.joblib`
- Model key:
  - `logistic_regression__wearable_only`
- Feature count:
  - 103 wearable-only features
- Test set:
  - 607 participant-day rows
  - 14 held-out participants
- Important interpretation rule:
  - Logistic Regression coefficients are predictive associations, not causal sleep drivers.

### 45. Final baseline validation results

- Held-out test metrics:
  - balanced accuracy: 0.8076
  - ROC AUC: 0.9046
  - F1: 0.7556
  - precision: 0.6996
  - recall: 0.8214
- Participant bootstrap:
  - repeats: 1,000
  - balanced accuracy median: 0.8040
  - balanced accuracy 95% interval: 0.7630 to 0.8333
  - ROC AUC median: 0.9066
  - ROC AUC 95% interval: 0.8815 to 0.9266
  - F1 median: 0.7549
  - F1 95% interval: 0.6335 to 0.8095
- Calibration note:
  - predicted probability deciles are directionally ordered
  - probability calibration should not yet be treated as final or high-stakes ready

### 46. Coefficient interpretation

- Strong positive coefficients for good-sleep prediction included:
  - `stress_score_mean`
  - `calories_record_count`
  - `hrv_detail_record_count`
  - `stress_calculation_failed_rate`
  - HRV high-frequency features
- Strong negative coefficients included:
  - `sedentary_minutes_sum`
  - `lightly_active_minutes_sum`
  - `stress_record_count`
  - `stress_responsiveness_points_mean`
  - `stress_exertion_points_mean`
- Caveat:
  - some large coefficients are count or missingness-related wearable availability signals
  - these should be reported separately from physiological interpretation

### 47. Updated notebook summary

- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Added:
  - final baseline validation script entry
  - selected baseline summary
  - coefficient preview
  - calibration table
  - participant bootstrap interval summary
  - final baseline validation report preview
  - validation figures
  - current pipeline status update

### 48. Validation

- Ran:
  - `python scripts/18_validate_interpret_baseline_model.py`
  - `python -m py_compile scripts/18_validate_interpret_baseline_model.py`
  - coefficient/calibration/bootstrap output checks
  - visual inspection of generated figures
  - notebook JSON validation
  - notebook code-cell syntax validation
  - notebook top-to-bottom execution check with `nbclient`
- Result:
  - Final baseline validation and interpretation completed successfully.
  - Notebook summary is updated and executable.

## Current Pipeline Status After Final Baseline Validation

```text
컬렉션별 EDA 완료
→ 필요한 변수 추출 완료
→ 날짜 단위로 집계 완료
→ 집계 데이터 검증 및 시각화 완료
→ 세 데이터 병합 완료
→ 최종 데이터셋 EDA 완료
→ 결측치 처리 완료
→ 범주형 인코딩 완료
→ 스케일링 완료
→ PCA 완료
→ participant-aware train/test split 완료
→ baseline modeling 완료
→ feature-set comparison 완료
→ tuned modeling 완료
→ final baseline validation and interpretation 완료
```

## Next Recommended Action After Final Baseline Validation

```text
advanced model comparison and final model selection
```

## Correction Note - Refocusing From Machine Learning To Deep Learning

### 49. Issue identified

- User clarified that this project should be treated as a deep learning project.
- Several recent modeling steps and next-step recommendations were framed around traditional machine learning rather than deep learning.
- The work is still useful as preprocessing and baseline comparison, but it should not be presented as completed deep learning modeling.

### 50. Work completed so far that remains valid for a deep learning project

- Valid and reusable:
  - MongoDB raw data access and collection inspection
  - collection-level EDA
  - needed variable extraction
  - participant-date daily aggregation
  - merged daily modeling dataset creation
  - final dataset EDA
  - leakage-aware exclusion of direct sleep outcome features
  - missing-value handling logic
  - feature column documentation
  - participant-aware split concept
  - baseline ML models as comparison references only
- These steps should be described as:
  - deep-learning data preparation
  - tabular daily dataset construction
  - traditional ML baseline/reference modeling

### 51. Parts that were machine-learning focused and need reframing

- PCA:
  - Previously treated as a standard preprocessing step before modeling.
  - For deep learning, PCA is not required by default.
  - It should be kept as exploratory dimensionality-reduction output only, not as the main deep learning input path.
- Baseline modeling:
  - `DummyClassifier`, `Logistic Regression`, `Logistic Regression + PCA`, and `Random Forest` are not deep learning models.
  - They should be retained only as non-deep-learning baseline references.
- Feature-set comparison:
  - `all_features`, `wearable_only`, `no_missing_indicators`, `no_availability_features`, and related Logistic/RF comparisons are traditional ML validation checks.
  - These results can inform feature policy, but they are not final deep learning model selection.
- Tuned modeling:
  - `GridSearchCV` over Logistic Regression and Random Forest is traditional ML tuning.
  - It should be described as tuning of baseline reference models, not as final modeling.
- Final baseline validation and interpretation:
  - Coefficients and RF importances are ML interpretability outputs.
  - They are useful for baseline interpretation, but they do not substitute for deep learning model interpretation.

### 52. Planned items that were machine-learning oriented and must be changed

- Previous plan:
  - advanced model comparison with XGBoost, LightGBM, CatBoost, SVM, and MLP
  - final model selection among tabular ML models
  - SHAP/permutation importance for tabular model interpretation
- Corrected deep learning plan:
  - build sequence dataset from participant-date daily data
  - preserve participant-wise temporal order
  - create rolling windows such as 7-day, 14-day, or 30-day sequences
  - predict next-day or current-day `good_sleep_label` after explicitly deciding the prediction task
  - train/validation/test split must remain participant-aware
  - scaler/imputer must be fit only on train data before generating model-ready tensors
  - train deep learning models such as:
    - MLP baseline on tabular daily features
    - 1D CNN on rolling windows
    - LSTM
    - GRU
    - Transformer encoder if data volume is sufficient
  - compare deep learning models against the already-created traditional ML baseline
  - select the final model from the deep learning comparison, not from Logistic Regression/Random Forest alone

### 53. Correct next starting point for deep learning

- Recommended restart point:
  - `data/processed/modeling_dataset_missing_handled.csv`
  - or `data/processed/modeling_dataset_encoded.csv`
- Reason:
  - these files preserve daily participant-level features before PCA and before ML-only modeling decisions
  - sequence construction needs participant-date rows sorted by time
- Next concrete task:
  - create a deep-learning sequence dataset script
- Proposed output:
  - `scripts/19_create_deep_learning_sequences.py`
  - `data/processed/deep_learning_sequences/window_7/`
  - `data/processed/deep_learning_sequences/window_14/`
  - sequence metadata report under `reports/`

### 54. Reporting correction

- Future reports should distinguish:
  - preprocessing completed
  - traditional ML baseline completed
  - deep learning modeling not yet completed
- The current selected baseline:
  - `wearable_only + Logistic Regression`
- Correct label:
  - traditional machine-learning baseline
- Incorrect label to avoid:
  - final model
  - deep learning model
  - completed deep learning result

## Corrected Next Recommended Action

```text
create deep-learning sequence dataset, then train MLP/1D-CNN/LSTM/GRU models
```
