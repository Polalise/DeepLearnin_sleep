# Work Log - 2026-06-29

## Deep Learning Refocus

### 1. Reason for update

- The project direction was clarified as deep learning.
- Previous modeling work had drifted toward traditional machine-learning planning.
- The following distinction is now explicit:

```text
Traditional ML models = baseline/reference only
Deep learning modeling = not yet completed
```

### 2. Updated project rules

- Updated:
  - `Codex.Rule.md`
- Added:
  - deep learning project refocus rules
  - PCA role clarification
  - traditional ML baseline/reference clarification
  - next core task as deep learning sequence dataset creation

### 3. Updated notebook summary

- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
- Added:
  - deep learning refocus section
  - warning that Logistic Regression and Random Forest are not deep learning models
  - PCA as exploratory feature-structure analysis
  - next script placeholder:
    - `scripts/19_create_deep_learning_sequences.py`
  - next workflow:

```text
participant-date daily table
-> participant/date sorting
-> rolling window sequence creation
-> train/validation/test tensors
-> MLP / 1D CNN / LSTM / GRU modeling
```

### 4. Corrected interpretation of previous work

- Still valid for deep learning:
  - MongoDB data loading
  - collection EDA
  - variable extraction
  - daily aggregation
  - merged participant-date dataset
  - missing-value handling
  - encoding
  - scaling
  - exploratory PCA
  - participant-aware split concept
- Must be framed as baseline/reference only:
  - DummyClassifier
  - Logistic Regression
  - Logistic Regression + PCA
  - Random Forest
  - feature-set comparison with Logistic Regression/Random Forest
  - tuned Logistic Regression/Random Forest
  - final baseline validation and coefficient interpretation

### 5. PCA role clarification

- PCA is retained as exploratory preprocessing analysis.
- PCA is useful for:
  - checking feature variance structure
  - checking redundancy/correlation patterns
  - inspecting feature-family clustering
  - visualizing PC-space target separation
- PCA is not the default main input path for deep learning.
- Deep learning sequence models should start from daily features before PCA unless explicitly testing a PCA-based experiment.

### 6. Correct next work

```text
Create deep-learning sequence dataset.
```

Recommended input:

```text
data/processed/modeling_dataset_missing_handled.csv
data/processed/modeling_dataset_encoded.csv
```

Recommended outputs:

```text
scripts/19_create_deep_learning_sequences.py
data/processed/deep_learning_sequences/window_7/
data/processed/deep_learning_sequences/window_14/
data/processed/deep_learning_sequences/window_30/
reports/deep_learning_sequence_dataset_summary.md
```

Recommended model path after sequence creation:

```text
MLP baseline
1D CNN
LSTM
GRU
```

### 7. Validation

- Ran:
  - notebook update with `nbformat`
  - notebook JSON validation
  - notebook code-cell syntax validation
  - notebook top-to-bottom execution check with `nbclient`
- Result:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb` is valid and executable.

## Suggested Opening Prompt For Next Conversation

```text
This is a deep learning project.
First read:
- C:\workSpace\DeepLearnin_sleep\Codex.Rule.md
- the latest log file under C:\workSpace\DeepLearnin_sleep\log
- C:\workSpace\DeepLearnin_sleep\notebooks\02_pipeline_from_scripts_summary.ipynb

Treat all previous Logistic Regression / Random Forest work as traditional ML baseline/reference only.
Treat PCA as exploratory feature-structure analysis, not as the default deep learning input.

Continue from deep learning sequence dataset creation:
- create scripts/19_create_deep_learning_sequences.py
- use participant-date daily data before PCA
- sort rows by participant and date
- build rolling windows such as 7, 14, and 30 days
- save train/validation/test tensors for MLP, 1D CNN, LSTM, and GRU experiments

Whenever files are created or modified, update log/YYYY-MM-DD.md and notebooks/02_pipeline_from_scripts_summary.ipynb.
```

## Deep Learning Sequence Dataset Creation

### 1. Reason for update

- Continued from the deep learning refocus task.
- Created the first deep-learning-ready sequence dataset from participant-date daily data before PCA.
- Logistic Regression and Random Forest remain traditional ML baseline/reference only.
- PCA remains exploratory feature-structure analysis and was not used as the default deep learning input.

### 2. Created / updated files

- Created:
  - `scripts/19_create_deep_learning_sequences.py`
  - `reports/deep_learning_sequence_dataset_summary.md`
  - `data/processed/deep_learning_sequences/feature_columns.csv`
  - `data/processed/deep_learning_sequences/deep_learning_participant_split_assignments.csv`
  - `data/processed/deep_learning_sequences/deep_learning_standard_scaler.joblib`
  - `data/processed/deep_learning_sequences/sequence_tensor_summary.csv`
  - `data/processed/deep_learning_sequences/window_7/`
  - `data/processed/deep_learning_sequences/window_14/`
  - `data/processed/deep_learning_sequences/window_30/`
- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
  - `log/2026-06-29.md`

### 3. Main implementation details

- Input:
  - `data/processed/modeling_dataset_encoded.csv`
  - `data/processed/encoded_feature_columns.csv`
  - `data/processed/splits/participant_split_assignments.csv`
- Rows are sorted by:
  - `participant_object_id`
  - `calendar_date`
- Validation participants are split from the original training participants.
- Feature scaling is fit on deep-learning train rows only, then applied to validation/test rows.
- Rolling windows are built for:
  - 7 days
  - 14 days
  - 30 days
- Window construction currently requires contiguous calendar dates.
- Each split `.npz` file contains:
  - `X_sequence`: samples x time_steps x features for 1D CNN, LSTM, and GRU
  - `X_mlp_flattened`: samples x time_steps*features for a window-flattened MLP
  - `X_mlp_current_day`: samples x features for daily tabular MLP baseline
  - `y`
  - `participant_object_id`
  - `window_start_date`
  - `window_end_date`
  - `feature_names`

### 4. Tensor summary

| window | split | samples | participants | sequence shape |
| ---: | --- | ---: | ---: | --- |
| 7 | train | 1,393 | 35 | `1393 x 7 x 197` |
| 7 | validation | 269 | 9 | `269 x 7 x 197` |
| 7 | test | 314 | 9 | `314 x 7 x 197` |
| 14 | train | 933 | 27 | `933 x 14 x 197` |
| 14 | validation | 146 | 7 | `146 x 14 x 197` |
| 14 | test | 214 | 5 | `214 x 14 x 197` |
| 30 | train | 435 | 19 | `435 x 30 x 197` |
| 30 | validation | 55 | 3 | `55 x 30 x 197` |
| 30 | test | 106 | 4 | `106 x 30 x 197` |

### 5. Validation

- Ran:
  - `python -m py_compile scripts/19_create_deep_learning_sequences.py`
  - `python scripts/19_create_deep_learning_sequences.py`
  - opened `data/processed/deep_learning_sequences/window_30/train.npz` and checked array names/shapes
  - loaded `notebooks/02_pipeline_from_scripts_summary.ipynb` as JSON
  - compiled all notebook code cells with `ast.parse`
  - loaded the new sequence summary and participant split summary from the notebook-added paths
- Result:
  - Script ran without warnings after dtype-safe scaling fix.
  - Notebook JSON loaded successfully.
  - Notebook code-cell syntax validation passed for 44 code cells.
  - Representative tensor check:
    - `X_sequence`: `(435, 30, 197)`, `float32`
    - `X_mlp_flattened`: `(435, 5910)`
    - `X_mlp_current_day`: `(435, 197)`
    - `y`: `(435,)`, `int64`

### 6. Correct next work

```text
Train initial deep learning experiments:
- MLP baseline
- 1D CNN
- LSTM
- GRU
```

## Deep Learning Project Roadmap And ML Baseline Reframing

### 1. Reason for update

- Some previous reports and notebook sections could be misread as if Logistic Regression / Random Forest work were the main project output.
- The project framing was corrected so traditional ML work is clearly marked as baseline/reference only.
- The full deep learning project roadmap was documented, including what is complete and what remains.

### 2. Created / updated files

- Created:
  - `reports/deep_learning_project_roadmap.md`
- Updated:
  - `reports/categorical_encoding_summary.md`
  - `reports/merge_summary.md`
  - `reports/final_dataset_eda.md`
  - `reports/missing_value_handling_summary.md`
  - `reports/scaling_summary.md`
  - `reports/pca_summary.md`
  - `reports/participant_split_summary.md`
  - `reports/baseline_modeling_summary.md`
  - `reports/feature_set_comparison_summary.md`
  - `reports/tuned_modeling_summary.md`
  - `reports/final_baseline_validation_report.md`
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
  - `log/2026-06-29.md`

### 3. Main corrections

- Reframed Logistic Regression / Random Forest / DummyClassifier outputs as traditional ML baseline/reference material.
- Reframed PCA as exploratory feature-structure analysis, not the default deep learning input.
- Reframed full-table scaling as exploratory/reference preprocessing; deep learning tensors use a train-only scaler created during sequence dataset generation.
- Changed next-step language from `scaling -> PCA -> baseline modeling` toward:

```text
participant-date daily data
-> participant-aware split
-> deep learning rolling sequence tensors
-> MLP / 1D CNN / LSTM / GRU experiments
```

### 4. Current project position

Completed:

```text
raw source orientation
collection EDA
daily aggregation
merged participant-date dataset
missing handling / feature finalization
exploratory PCA
traditional ML reference baselines
deep learning sequence tensor creation
```

Not yet completed:

```text
MLP training
1D CNN training
LSTM training
GRU training
deep learning model comparison report
final deep learning model report
```

### 5. Roadmap next scripts

Recommended next scripts:

```text
scripts/20_prepare_deep_learning_loaders.py
scripts/21_train_mlp_baselines.py
scripts/22_train_sequence_models.py
scripts/23_compare_deep_learning_models.py
```

### 6. Validation

- Loaded `notebooks/02_pipeline_from_scripts_summary.ipynb` as JSON after roadmap update.
- Compiled all notebook code cells with `ast.parse`.
- Cleared stale notebook stored outputs so old ML-centric wording is not preserved in cached outputs.
- Re-searched reports for stale wording around PCA, baseline, final model, and deep learning positioning.
- Result:
  - Notebook JSON load passed.
  - Notebook code-cell syntax validation passed for 45 code cells.
  - Notebook stored outputs count is `0`.
  - Stale wording search for `scaling -> PCA`, `Best Tuned Candidate`, `# Final Baseline Validation and Interpretation`, and `PCA dimensionality reduction` returned no remaining matches in reports/notebook.

## Pre-Modeling Design Report

### 1. Reason for update

- The project needs a clear pre-modeling design explanation before deep learning training.
- EDA, preprocessing, missing-value handling, and PCA decisions were consolidated into one report.
- The report clarifies how earlier work supports the deep learning sequence-modeling path.

### 2. Created / updated files

- Created:
  - `reports/pre_modeling_design_report.md`
- Updated:
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
  - `log/2026-06-29.md`

### 3. Main contents

- EDA design:
  - participant-date unit
  - dataset shape
  - target balance
  - missingness by feature family
  - target correlations
  - outlier checks
  - leakage-prone sleep outcome columns
- Preprocessing design:
  - daily merge
  - missing handling
  - feature finalization / encoding
  - participant-aware split
  - deep learning sequence tensor creation
- Missing-value design:
  - drop columns over 70% missingness
  - add missing indicators
  - zero-fill count/rate/sum/record-count features
  - median-fill other numeric features
  - exclude direct same-night sleep outcome columns
- PCA design:
  - exploratory only
  - 187 scaled input features
  - 38 PCs for 95.05% cumulative variance
  - PC1/PC2 dominated by availability/missingness structure
  - PC3 dominated by HRV physiological values
  - PCA loadings are not supervised feature importance

### 4. Current conclusion

```text
Pre-modeling design is complete enough to proceed to deep learning training.
Deep learning model training has not started yet.
```

## Deep Learning Model Candidate Expansion And Comparison Roadmap

### 1. Reason for update

- The sequence model candidate list needed to include `SimpleRNN` and `BiLSTM` in addition to LSTM, GRU, and 1D CNN.
- A model comparison roadmap was needed before training starts so selection does not depend on ad hoc test-set results.

### 2. Created / updated files

- Created:
  - `reports/deep_learning_model_comparison_roadmap.md`
- Updated:
  - `Codex.Rule.md`
  - `reports/deep_learning_project_roadmap.md`
  - `reports/pre_modeling_design_report.md`
  - `reports/deep_learning_sequence_dataset_summary.md`
  - `reports/baseline_modeling_summary.md`
  - `reports/tuned_modeling_summary.md`
  - `reports/final_baseline_validation_report.md`
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
  - `log/2026-06-29.md`

### 3. Updated candidate models

Deep learning baseline models:

```text
MLP current-day
MLP flattened-window
```

Sequence models:

```text
SimpleRNN
LSTM
GRU
BiLSTM
1D CNN
```

### 4. Comparison plan

- Use validation metrics for model family, window length, and hyperparameter selection.
- Evaluate the selected candidate once on the held-out participant test split.
- Use balanced accuracy as the primary metric.
- Track ROC AUC, average precision, F1, precision, recall, and confusion matrix as secondary metrics.
- Compare against `wearable_only + Logistic Regression` as the traditional ML reference baseline only.
- Use participant-level bootstrap uncertainty before calling any model final.

### 5. Current status

```text
Model comparison roadmap is defined.
Deep learning model training has not started yet.
```

### 6. Validation

- Loaded `notebooks/02_pipeline_from_scripts_summary.ipynb` as JSON.
- Compiled all notebook code cells with `ast.parse`.
- Confirmed notebook stored outputs remain cleared.
- Searched project reports/notebook/rules for `SimpleRNN` and `BiLSTM` inclusion.
- Result:
  - Notebook JSON load passed.
  - Notebook code-cell syntax validation passed for 47 code cells.
  - Notebook stored outputs count is `0`.
  - `SimpleRNN` and `BiLSTM` are now included in the project rule, roadmap, model-comparison roadmap, pre-modeling report, sequence dataset summary, and notebook.

## Manual Deep Learning Experiment Notebook

### 1. Reason for update

- The user will directly run and validate deep learning experiments.
- A separate notebook was created so long-running training outputs do not clutter the pipeline summary notebook.
- Code was written with comments and safe execution switches; training was not run by Codex.

### 2. Created / updated files

- Created:
  - `notebooks/03_deep_learning_model_experiments.ipynb`
- Updated:
  - `reports/deep_learning_model_comparison_roadmap.md`
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
  - `log/2026-06-29.md`

### 3. Notebook contents

- Load and inspect `data/processed/deep_learning_sequences/window_7|14|30/*.npz`.
- Check participant overlap across train/validation/test.
- Build PyTorch DataLoaders.
- Define models:
  - MLP current-day
  - MLP flattened-window
  - SimpleRNN
  - LSTM
  - GRU
  - BiLSTM
  - 1D CNN
- Train with early stopping and class imbalance handling.
- Rank candidates by validation metrics.
- Compare held-out test metrics against `wearable_only + Logistic Regression`.
- Provide participant-level bootstrap skeleton for the selected candidate.

### 4. Execution safety

- Default:

```text
RUN_FULL_EXPERIMENTS = False
RUN_SMOKE_TEST = True
```

- The user can first run the smoke test, then set `RUN_FULL_EXPERIMENTS = True` for the full 21-run comparison.

### 5. Validation

- Codex did not run model training.
- Loaded the new notebook as JSON.
- Compiled all new notebook code cells with `ast.parse`.
- Confirmed stored output count is `0`.

## UTF-8 Notebook And Python Documentation Cleanup

### 1. Reason for update

- Some notebook comments and markdown cells contained broken Korean text represented as repeated `?` characters.
- The user requested Python and notebook documents to use UTF-8 going forward.

### 2. Created / updated files

- Updated:
  - `Codex.Rule.md`
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
  - `notebooks/03_deep_learning_model_experiments.ipynb`
  - `log/2026-06-29.md`

### 3. Main changes

- Rewrote `Codex.Rule.md` as clean UTF-8 text.
- Added explicit encoding rules:
  - Python scripts, notebooks, markdown reports, and logs must be UTF-8.
  - Python text reads/writes should use `encoding="utf-8"` unless a format requires otherwise.
  - `utf-8-sig` is acceptable for CSV files intended for spreadsheet tools.
  - CP949/EUC-KR/terminal-default encodings should not be used for notebooks or Python files.
- Rewrote corrupted markdown cells in `notebooks/02_pipeline_from_scripts_summary.ipynb`.
- Rewrote corrupted comments/messages in `notebooks/03_deep_learning_model_experiments.ipynb`.

### 4. Validation

- Loaded the following files with `encoding="utf-8"`:
  - `Codex.Rule.md`
  - `notebooks/02_pipeline_from_scripts_summary.ipynb`
  - `notebooks/03_deep_learning_model_experiments.ipynb`
- Confirmed:
  - replacement character count: `0`
  - repeated `???` count: `0`
  - `02_pipeline_from_scripts_summary.ipynb`: 81 cells, 47 code cells syntax-checked, 0 stored outputs
  - `03_deep_learning_model_experiments.ipynb`: 18 cells, 17 code cells syntax-checked, 0 stored outputs

## Korean UTF-8 Comments For Experiment Notebook

### 1. Reason for update

- The user requested Korean comments before running the deep learning experiment notebook.
- The notebook comments and user-facing guidance were rewritten in Korean UTF-8.
- Model training was not run.

### 2. Updated files

- `notebooks/03_deep_learning_model_experiments.ipynb`
- `log/2026-06-29.md`

### 3. Main changes

- Rewrote markdown guidance in Korean.
- Rewrote code comments in Korean.
- Rewrote user-facing print messages in Korean.
- Kept code logic unchanged.
- Kept `RUN_FULL_EXPERIMENTS = False` and `RUN_SMOKE_TEST = True`.

### 4. Validation

- Loaded `notebooks/03_deep_learning_model_experiments.ipynb` with `encoding="utf-8"`.
- Compiled all notebook code cells with `ast.parse`.
- Confirmed:
  - replacement character count: `0`
  - repeated `???` count: `0`
  - total question mark count: `0`
  - notebook cells: `18`
  - code cells syntax-checked: `17`
  - stored outputs: `0`
- Verified Korean text as Unicode code points using `unicode_escape`.

## Deep Learning Data Diagnostics And Feature Subset Planning

### 1. Reason for update

- The initial deep learning experiment results showed a large validation-to-test performance gap.
- The user manually ran diagnostic notebook cells provided by Codex to check whether the issue is likely model capacity or data/split design.
- Notebook summary updates are intentionally deferred until a new experiment notebook is created.

### 2. Diagnostics completed by user

- Confirmed sleep target date semantics:
  - `calendar_date` matches sleep `endTime` for all target rows.
  - About 38% of sleep records cross midnight.
  - Daily feature keys and target keys match exactly.
- Reviewed source coverage and leakage-risk feature families.
- Quantified risk groups in the current 197-feature encoded dataset.
- Quantified train/validation/test distribution shift using standardized mean difference.

### 3. Main findings

- The held-out test split differs materially from train in feature coverage and missingness.
- The largest shifts are in:
  - high-risk sleep-session missing indicators
  - participant survey missing indicators
  - high-risk sleep-session physiological features
  - some same-date daytime activity features
- Test participant-level target mean is lower than train:
  - train participant target mean: approximately `0.376`
  - validation participant target mean: approximately `0.360`
  - test participant target mean: approximately `0.324`
- The current validation/test gap is therefore likely affected by participant-level distribution shift and feature-coverage shift, not only model architecture.

### 4. Feature subset planning

- Created ablation feature subset definitions for future deep learning comparison:

| subset_name | feature_count | purpose |
| --- | ---: | --- |
| `full_current` | 197 | current full feature set, for reproduction/reference |
| `no_high_sleep_session` | 127 | remove HRV, SpO2, respiratory, wrist-temperature feature families and their missing indicators |
| `no_high_sleep_session_no_stress` | 115 | remove sleep-session and stress/recovery feature families |
| `daytime_context_survey` | 115 | use daytime activity, SEMA/context, participant survey, and resting HR |
| `daytime_only` | 19 | conservative activity/resting-HR-only comparison |

### 5. Created files

- Created by user-side notebook execution:
  - `data/processed/deep_learning_feature_subsets/full_current_features.csv`
  - `data/processed/deep_learning_feature_subsets/no_high_sleep_session_features.csv`
  - `data/processed/deep_learning_feature_subsets/no_high_sleep_session_no_stress_features.csv`
  - `data/processed/deep_learning_feature_subsets/daytime_context_survey_features.csv`
  - `data/processed/deep_learning_feature_subsets/daytime_only_features.csv`

### 6. Next planned work

- Create a reusable script for subset-specific sequence tensor generation:
  - proposed path: `scripts/20_create_deep_learning_sequences_by_feature_subset.py`
- Generate sequence tensors under:
  - `data/processed/deep_learning_sequences_by_subset/`
- Update `notebooks/02_pipeline_from_scripts_summary.ipynb` later when the new notebook/script workflow is formalized.

## Feature Subset Sequence Tensor Generation

### 1. Reason for update

- The feature subset CSV files were used to create ablation-ready deep learning sequence tensors.
- The user executed the subset sequence generation script manually and reported the summary output.
- Notebook summary updates remain deferred until the next notebook workflow is created.

### 2. Created script

- Created manually from Codex-provided code:
  - `scripts/20_create_deep_learning_sequences_by_feature_subset.py`

### 3. Created outputs

- Created:
  - `data/processed/deep_learning_sequences_by_subset/`
  - `data/processed/deep_learning_sequences_by_subset/sequence_tensor_summary_by_subset.csv`

For each subset, the output includes:

```text
feature_columns.csv
standard_scaler.joblib
metadata.json
window_7/train.npz, validation.npz, test.npz
window_14/train.npz, validation.npz, test.npz
window_30/train.npz, validation.npz, test.npz
sample_index.csv per window
```

### 4. Generated feature subsets

| subset_name | features |
| --- | ---: |
| `full_current` | 197 |
| `no_high_sleep_session` | 127 |
| `no_high_sleep_session_no_stress` | 115 |
| `daytime_context_survey` | 115 |
| `daytime_only` | 19 |

### 5. Sample count validation

The subset-specific tensors preserve the same samples as the original full-current sequence tensors:

| window | train | validation | test |
| ---: | ---: | ---: | ---: |
| 7 | 1393 | 269 | 314 |
| 14 | 933 | 146 | 214 |
| 30 | 435 | 55 | 106 |

### 6. Current interpretation

- Feature dimensions now differ by subset, but row/window composition is held constant.
- This makes the next deep learning comparison suitable for ablation:

```text
same participants/windows/splits
different feature families
same model families and validation-first selection rule
```

### 7. Next planned work

- Validate the generated subset tensor files by loading representative `.npz` files.
- Then create a new notebook workflow for subset-aware model comparison.

## Feature Subset Tensor Validation And Phase 1A Grid

### 1. Reason for update

- The user validated all generated feature-subset sequence tensors before starting model training.
- A smaller first-pass experiment grid was created to avoid running all 105 planned subset/window/model combinations at once.

### 2. Tensor validation result

- Validated:
  - `.npz` existence
  - feature count consistency
  - sample count consistency
  - sequence tensor shapes
  - MLP flattened/current-day tensor shapes
  - NaN/Inf absence
- Result:
  - `problem rows: 0`
  - no NaN in `X_sequence`
  - no Inf in `X_sequence`
  - all feature counts matched
  - all sample counts matched
  - all expected shapes matched

### 3. Full subset-aware experiment grid

- Planned full grid:

```text
5 feature subsets x 3 windows x 7 model families = 105 experiments
```

- Phase split:

| phase | experiments | models |
| --- | ---: | --- |
| `phase_1_priority` | 60 | `mlp_current_day`, `gru`, `bilstm`, `cnn_1d` |
| `phase_2_follow_up` | 45 | `mlp_flattened_window`, `simple_rnn`, `lstm` |

### 4. Phase 1A grid created

- Created by user-side notebook execution:
  - `data/processed/deep_learning_subset_experiments/phase_1a_experiment_grid.csv`

- Phase 1A includes:

| item | values |
| --- | --- |
| feature subsets | `full_current`, `no_high_sleep_session`, `daytime_only` |
| windows | `7`, `14`, `30` |
| models | `mlp_current_day`, `gru`, `bilstm`, `cnn_1d` |
| total experiments | `36` |

### 5. Current interpretation

- Phase 1A is the recommended next run because it directly compares:
  - current full feature behavior
  - removal of sleep-session-derived feature families
  - conservative daytime/resting-HR-only behavior
- Validation metrics should still drive model/window/subset selection.
- Test metrics should only be interpreted after validation-based selection.

## Phase 1A Subset Ablation Training Run

### 1. Reason for update

- The user executed the first subset-aware deep learning training batch.
- The run used the Phase 1A grid to compare three feature subsets across three windows and four model families.

### 2. Run configuration

- Experiment grid:
  - `data/processed/deep_learning_subset_experiments/phase_1a_experiment_grid.csv`
- Output directory:
  - `data/processed/deep_learning_subset_experiments/phase_1a_outputs/`
- Models:
  - `mlp_current_day`
  - `gru`
  - `bilstm`
  - `cnn_1d`
- Feature subsets:
  - `daytime_only`
  - `full_current`
  - `no_high_sleep_session`
- Windows:
  - `7`
  - `14`
  - `30`
- Total experiments:
  - `36`

### 3. Created outputs

- Created or updated by user-side notebook execution:
  - `data/processed/deep_learning_subset_experiments/phase_1a_outputs/phase_1a_model_metrics.csv`
  - `data/processed/deep_learning_subset_experiments/phase_1a_outputs/phase_1a_training_history.csv`
  - `data/processed/deep_learning_subset_experiments/phase_1a_outputs/phase_1a_model_predictions.csv`

### 4. Execution result

- All 36 Phase 1A experiments completed and were saved.
- The final executed experiment was:
  - `phase1a_035`
  - subset: `no_high_sleep_session`
  - window: `30`
  - model: `mlp_current_day`
- Console Korean text was partially mojibake in the terminal, but saved experiment outputs appear to have been written successfully.

### 5. Next planned work

- Load `phase_1a_model_metrics.csv`.
- Rank candidates by validation balanced accuracy.
- Only after validation ranking, inspect the paired test metrics for the selected/top validation candidates.
- Compare subset behavior to determine whether removing high-risk sleep-session features improves generalization.

## Previous-Day Feature Dataset And Sequence Tensors

### 1. Reason for update

- Started the second data improvement pass.
- The goal is to reduce same-date temporal ambiguity by using features from `target_date - 1 day` for each sleep target.
- The user executed the previous-day dataset and sequence tensor creation cells manually.

### 2. Created outputs

- Created:
  - `data/processed/deep_learning_previous_day/modeling_dataset_previous_day_encoded.csv`
  - `data/processed/deep_learning_previous_day/previous_day_feature_columns.csv`
  - `data/processed/deep_learning_previous_day/sequences_by_subset/`
  - `data/processed/deep_learning_previous_day/sequences_by_subset/sequence_tensor_summary_previous_day_by_subset.csv`

### 3. Previous-day dataset summary

- Original encoded rows: `3551`
- Previous-day usable rows: `3116`
- Coverage: approximately `87.75%`
- Feature lag:
  - all usable rows have `feature_lag_days = 1`
- Split rows:
  - train: `2135`
  - validation: `457`
  - test: `524`
- Participants:
  - train: `41`
  - validation: `10`
  - test: `12`

### 4. Previous-day sequence tensor summary

- Feature subsets were generated for:
  - `full_current`
  - `no_high_sleep_session`
  - `no_high_sleep_session_no_stress`
  - `daytime_context_survey`
  - `daytime_only`
- Windows generated:
  - `7`
  - `14`
  - `30`
- Sample counts are consistent across feature subsets:

| window | train | validation | test |
| ---: | ---: | ---: | ---: |
| 7 | 1303 | 245 | 296 |
| 14 | 887 | 136 | 206 |
| 30 | 415 | 52 | 101 |

### 5. Current interpretation

- The previous-day tensor set is stricter than the same-date tensor set.
- It preserves enough window-7 and window-14 samples for follow-up experiments.
- Window 30 remains small, especially validation/test participant counts, so it should be interpreted cautiously.

## Previous-Day Phase 2A Training Run

### 1. Reason for update

- The user validated previous-day subset tensors and created the Phase 2A experiment grid.
- The user then executed all Phase 2A previous-day deep learning experiments manually.

### 2. Tensor validation

- Previous-day tensor validation result:
  - `problem rows: 0`
  - no NaN values
  - no Inf values
  - feature counts matched
  - sample counts matched
  - sequence shapes matched

### 3. Phase 2A grid

- Created:
  - `data/processed/deep_learning_previous_day/experiments/phase_2a_previous_day_experiment_grid.csv`
- Experiment count:
  - `16`
- Subsets:
  - `daytime_only`
  - `no_high_sleep_session`
- Windows:
  - `7`
  - `14`
- Models:
  - `mlp_current_day`
  - `gru`
  - `bilstm`
  - `cnn_1d`

### 4. Created outputs

- Created or updated:
  - `data/processed/deep_learning_previous_day/experiments/phase_2a_outputs/phase_2a_previous_day_model_metrics.csv`
  - `data/processed/deep_learning_previous_day/experiments/phase_2a_outputs/phase_2a_previous_day_training_history.csv`
  - `data/processed/deep_learning_previous_day/experiments/phase_2a_outputs/phase_2a_previous_day_model_predictions.csv`

### 5. Execution result

- All 16 Phase 2A previous-day experiments completed and were saved.
- The final executed experiment was:
  - `phase2a_015`
  - subset: `no_high_sleep_session`
  - window: `14`
  - model: `cnn_1d`

### 6. Next planned work

- Rank Phase 2A candidates by validation balanced accuracy.
- Pair validation-selected candidates with held-out test metrics.
- Compare previous-day results against Phase 1A same-date subset results.
- Interpret whether stricter previous-day feature timing improves reliability or reduces apparent performance.

## Same-Date Versus Previous-Day Comparison

### 1. Reason for update

- The user completed threshold-tuned comparison between same-date Phase 1A results and previous-day Phase 2A results.
- This comparison is central to interpreting whether the model is a prospective previous-day prediction model or a same-date classification model.

### 2. Created comparison outputs

- Created by user-side notebook execution:
  - `data/processed/deep_learning_previous_day/experiments/phase_2a_outputs/same_date_vs_previous_day_threshold_comparison.csv`
  - `data/processed/deep_learning_previous_day/experiments/phase_2a_outputs/same_date_vs_previous_day_top_test_candidates.csv`

### 3. Test-set comparison summary

| feature_timing | subset_name | mean test balanced accuracy | max test balanced accuracy | mean test ROC AUC | max test ROC AUC |
| --- | --- | ---: | ---: | ---: | ---: |
| `same_date` | `daytime_only` | `0.7635` | `0.8440` | `0.8450` | `0.9047` |
| `same_date` | `no_high_sleep_session` | `0.6671` | `0.8146` | `0.7253` | `0.8783` |
| `same_date` | `full_current` | `0.5941` | `0.7290` | `0.6522` | `0.8114` |
| `previous_day` | `daytime_only` | `0.5577` | `0.6098` | `0.5889` | `0.6286` |
| `previous_day` | `no_high_sleep_session` | `0.4772` | `0.5486` | `0.4871` | `0.5661` |

### 4. Current best same-date candidate

- Candidate:
  - `phase1a_003`
  - feature timing: `same_date`
  - subset: `daytime_only`
  - window: `7`
  - model: `mlp_current_day`
  - selected threshold from validation: `0.42`
- Test metrics:
  - balanced accuracy: `0.8440`
  - ROC AUC: `0.9023`
  - average precision: `0.8402`
  - F1: `0.8215`

### 5. Main interpretation

- The strongest performance comes from `same_date + daytime_only`, not from previous-day features.
- When features are restricted to `target_date - 1 day`, performance drops substantially.
- This suggests that much of the predictive signal in the same-date experiments may come from same-date/daytime state or sleep-adjacent timing rather than strictly prior-day prospective information.
- The same-date best model should be interpreted as a same-date classification model, not as a strict previous-day sleep forecasting model.

### 6. Next planned work

- Generate a concise Phase 1A/Phase 2A result report.
- Clearly separate:
  - best same-date classifier
  - previous-day prospective-feature experiment
  - limitations around timing, participant split size, and window-30 small sample counts

## Subset Ablation And Previous-Day Report

### 1. Reason for update

- The user generated a concise report summarizing Phase 1A same-date subset ablation and Phase 2A previous-day timing experiments.
- The report records the current best same-date classifier and the performance drop under stricter previous-day timing.

### 2. Created report

- Created:
  - `reports/deep_learning_subset_ablation_and_previous_day_report.md`

### 3. Report contents

- Best current same-date candidate:
  - `phase1a_003`
  - feature timing: `same_date`
  - subset: `daytime_only`
  - window: `7`
  - model: `mlp_current_day`
  - validation-selected threshold: `0.42`
  - test balanced accuracy: approximately `0.8440`
  - test ROC AUC: approximately `0.9023`
  - test F1: approximately `0.8215`
- Comparison finding:
  - same-date `daytime_only` substantially outperformed previous-day `daytime_only`.
  - previous-day features are more temporally conservative but much weaker predictors in the current dataset.

### 4. Interpretation recorded

- The current best model should be framed as a same-date sleep classification model.
- It should not be described as a strict previous-day forecasting model.
- Previous-day results should be reported as timing sensitivity analysis.

### 5. Next planned work

- Run participant-level bootstrap confidence intervals for selected candidates.
- Update the main pipeline summary notebook after final reporting structure is fixed.
